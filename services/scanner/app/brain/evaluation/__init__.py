"""The ruler's pure math: metric statistics, paired comparison, promotion gates.

Everything here judges stored evidence; nothing here may influence decision
production. Changes to this package bump ``RULER_VERSION`` (see
``app.brain.identity``) and never rotate prediction campaigns.
"""
