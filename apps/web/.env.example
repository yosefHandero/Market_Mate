NEXT_PUBLIC_SCANNER_API_BASE=http://127.0.0.1:8005

# Server-side only. Required when the scanner has PUBLIC_READ_ACCESS_ENABLED=false (recommended for serious deployments).
SCANNER_READ_API_TOKEN=

# Required for journal create/update proxy routes.
SCANNER_ADMIN_API_TOKEN=
