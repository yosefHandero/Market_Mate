NEXT_PUBLIC_SCANNER_API_BASE=http://127.0.0.1:8005

# Server-side only. Must match services/scanner READ_API_TOKEN when PUBLIC_READ_ACCESS_ENABLED=false.
SCANNER_READ_API_TOKEN=

# Server-side only. Must match services/scanner ADMIN_API_TOKEN for journal writes and optional reconciliation checks.
SCANNER_ADMIN_API_TOKEN=
