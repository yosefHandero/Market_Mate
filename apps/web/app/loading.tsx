export default function Loading() {
  return (
    <main>
      <section className="card">
        <h1>Loading dashboard...</h1>
        <p className="muted">Fetching scanner data.</p>
      </section>
    </main>
  );
}
