{
  "name": "market-mate-scanner",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start",
    "lint": "npm --prefix apps/web run lint",
    "test:web": "npm --prefix apps/web run test",
    "check:web": "npm --prefix apps/web run check",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  },
  "description": "Market Mate Scanner monorepo root. Primary UI lives in apps/web; backend in services/scanner.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yosefHandero/Market_Mate.git"
  },
  "license": "ISC"
}
